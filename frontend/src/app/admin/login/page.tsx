import { PlatformAdminLoginForm } from './PlatformAdminLoginForm';

export const metadata = { title: 'Masuk — Platform Admin', robots: 'noindex' };

export default function AdminLoginPage() {
  return <PlatformAdminLoginForm />;
}
