# API
NEXT_PUBLIC_API_URL=http://localhost:4000
API_URL=http://localhost:4000

# Domain
NEXT_PUBLIC_BASE_DOMAIN=lvh.me
# NEXT_PUBLIC_ROOT_DOMAIN=lentera.id  ← set in production only; platform is localhost in dev

# Auth (public keys only — never put secrets here)
NEXT_PUBLIC_MIDTRANS_CLIENT_KEY=SB-Mid-client-xxxxxxxxxxxx
